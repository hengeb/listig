{layout '../templates/layout.latte'}

{block title}{$translator->trans('login.title', ['%app_name%' => $appName])}{/block}

{block content}
<div class="narrow-page-sm">
    <div class="card">
        <h1 class="centered-heading">{$translator->trans('login.heading')}</h1>

        <div class="flash" n:if="isset($flash) && $flash">{$flash|escapeHtml}</div>

        {if $oidcEnabled}
            <a href="/_/login/oidc" class="btn btn-primary btn-block oidc-login-btn">{$translator->trans('auth.oidc_login_button')}</a>
            <div class="login-divider">{$translator->trans('auth.oidc_divider')}</div>
        {/if}

        <form method="post" action="/_/login">
            <label class="form-label" for="email">{$translator->trans('login.email_label')}</label>
            <input
                type="email"
                id="email"
                name="email"
                required
                autofocus
                class="form-input"
            >
            <button type="submit" class="btn btn-primary btn-block">{$translator->trans('login.submit')}</button>
        </form>
    </div>
</div>
{/block}
