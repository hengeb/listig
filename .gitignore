# Composer
/vendor/

# Environment — secrets must never be committed
.env
config/config.yml

# Generated from deploy/*.example by an operator following the Quick start —
# compose.yml/config.yml are not secrets themselves, but operator-specific and
# not meant to be committed back into the repo; .env always is
deploy/compose.yml
deploy/.env
deploy/config.yml

# Latte template cache
/var/
/tmp/latte/

# Logs
*.log

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# PHP
*.cache

